import {Tabs} from 'nextra/components'
import {RestfulApiRequest} from "../../components/restful-api/restful-api-request";
import {RestfulApiTable} from "../../components/restful-api/restful-api-table";
import {RestfulApiContainer} from "../../components/restful-api/restful-api-container";
import {RestfulApiResponse} from "../../components/restful-api/restful-api-response";
import {
    BODY_COLUMNS,
    HEADERS_COLUMNS,
    HEADERS_ROWS,
    HTTP_RESPONSE_COLUMNS,
    HTTP_RESPONSE_ROWS, HTTP_RESPONSE_ROWS_DELETE
} from "../../constants/restful-api";

## Get Group Struct

<RestfulApiContainer>
    <div>
        The "Get Group Struct" endpoint return the structure of parameters of specified Group

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Get Group Struct"`}
            method="get"
            path="/api/lib/group/{group_id}/struct"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/group/{group_id}/struct' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.group.struct(groupId,config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const requestOptions = {
                        method: "GET",
                        headers: myHeaders,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/group/{group_id}/struct", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with Group struct"
        >
            ```json
            "content": {
                "label": "Arke auth member",
                "parameters": [
                {
                    "default": null,
                    "helper_text": null,
                    "id": "email",
                    "label": "Email",
                    "max_length": 50,
                    "min_length": 0,
                    "multiple": false,
                    "required": false,
                    "strip": false,
                    "type": "string",
                    "values": null
                },
                ...
                ]
            },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>

## Get All Arke of Group

<RestfulApiContainer>
    <div>
        The "Get All Arke of Group" endpoint return the list of all available arke inside the group

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Get All Arke of Group"`}
            method="get"
            path="/api/lib/group/{group_id}/arke"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/group/{group_id}/arke' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.group.getAllArke(config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const requestOptions = {
                    method: "GET",
                    headers: myHeaders,
                    redirect: "follow"
                };

                    fetch("https://arkehub.com/api/lib/group/{group_id}/arke", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with Arke list in the group"
        >
            ```json
            {
                "content": {
                    "count": 5,
                    "items": [
                        {
                            "active": true,
                            "arke_id": "arke",
                            "id": "super_admin",
                            "inserted_at": "2023-09-14T10:03:48.000000Z",
                            "label": "Super Admin",
                            "metadata": {},
                            "parameters": [],
                            "remote": false,
                            "type": "arke",
                            "updated_at": "2023-09-14T10:03:48.000000Z"
                        },
                        ...
                    ]
                },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>

## Get All Units of Group

<RestfulApiContainer>
    <div>
        The "Get All Units of Group" endpoint return the list of all available units inside the group

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Get All Arke of Group"`}
            method="get"
            path="/api/lib/group/{group_id}/unit"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/group/{group_id}/unit' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                        -H 'Content-Type: application/json' \
                        ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.group.getAllUnits(config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                        myHeaders.append("Content-Type", "application/json");

                        const requestOptions = {
                            method: "GET",
                            headers: myHeaders,
                            redirect: "follow"
                        };

                        fetch("https://arkehub.com/api/lib/group/{group_id}/unit", requestOptions)
                        .then((response) => response.text())
                        .then((result) => console.log(result.data))
                        .catch((error) => console.error(error));
                        ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with Unit list in the group"
        >
            ```json
            {
                "content": {
                    {
                        "arke_id": "super_admin",
                        "arke_system_user": "c2e3b048-1a6a-11ee-908c-1a84faf9f6ef",
                        "business_name": "Arke",
                        "email": "user@arkehub.com",
                        "id": "824135a6-52e6-11ee-9387-96e316d04b51",
                        "last_access_time": "2024-04-04T15:39:48.501632Z",
                        "last_name": "Super",
                        "metadata": {},
                        "phone": "123123123",
                        "postal_code": null,
                        "updated_at": "2024-04-04T15:39:49.000000Z",
                        "vat": "123123123"
                    },
                },
                    "messages": []
                }
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>

## Get Unit in a Group

<RestfulApiContainer>
    <div>
        The "Get Unit in a Group" endpoint return details of the specified unit in the group

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Get Unit in a Group"`}
            method="get"
            path="/api/lib//group/{group_id}/unit/{unit_id}"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/group/{group_id}/unit/{unit_id}' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.group.getUnit(groupId, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const requestOptions = {
                        method: "GET",
                        headers: myHeaders,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/group/{group_id}/unit/{unit_id}", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with Unit details of group"
        >
            ```json
            {
                "content": {
                    {
                        "active": true,
                        "arke_id": "arke",
                        "id": "super_admin",
                        "inserted_at": "2023-09-14T10:03:48.000000Z",
                        "label": "Super Admin",
                        "metadata": {},
                        "parameters": [],
                        "remote": false,
                        "type": "arke",
                        "updated_at": "2023-09-14T10:03:48.000000Z"
                    },
                },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>

# Create Group

<RestfulApiContainer>
    <div>
        The "Create Group" endpoint create a Group with the specified information

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### Body
        <RestfulApiTable
            columns={BODY_COLUMNS}
            rows={[
                {key: '<PARAMETER_ID>', value: '<VALUE>'},
                {key: '<PARAMETER_ID_2>', value: '<VALUE>'},
                {key: '<PARAMETER_ID_3>', value: '<VALUE>'}
            ]}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Create Group"`}
            method="post"
            path="/api/lib/group/unit"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/group/unit' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                        -H 'Content-Type: application/json' \
                        ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.group.create(groupId, data, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const raw = JSON.stringify(data);

                    const requestOptions = {
                        method: "POST",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/group/unit", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with Group details created"
        >
            ```json
            {
                "content": {
                    "arke_id": "group",
                    "arke_list": ["arke_1","arke_2"],
                    "description": "Group",
                    "id": "docs_group",
                    "inserted_at": "2024-04-04T17:11:41Z",
                    "label": "Group docs",
                    "metadata": {},
                    "updated_at": "2024-04-04T17:11:41Z"
                },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>


## Edit Group

<RestfulApiContainer>
    <div>
        The "Edit Group" endpoint update the specified Group information

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### Body
        <RestfulApiTable
            columns={BODY_COLUMNS}
            rows={[
                {key: '<PARAMETER_ID>', value: '<NEW_VALUE>'}
            ]}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Edit Group"`}
            method="put"
            path="/api/lib/group/unit/{group_id}"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/group/unit/{group_id}' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                        -H 'Content-Type: application/json' \
                        ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.group.edit(groupId, data, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const raw = JSON.stringify(data);

                    const requestOptions = {
                        method: "PUT",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/group/unit/{group_id}", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with Group details updated"
        >
            ```json
            {
                "content": {
                "arke_id": "group",
                "arke_list": ["arke_1","arke_2"],
                "description": "Edited Group",
                "id": "docs_group",
                "inserted_at": "2024-04-04T17:11:41Z",
                "label": "Edited Group docs",
                "metadata": {},
                "updated_at": "2024-04-04T17:11:41Z"
            },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>

## Delete Group

<RestfulApiContainer>
    <div>
        The "Delete Group" endpoint delete the specified group

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### Body
        <RestfulApiTable
            columns={BODY_COLUMNS}
            rows={[
                {key: '<PARAMETER_ID>', value: '<NEW_VALUE>'}
            ]}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS_DELETE}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Delete Group"`}
            method="delete"
            path="/api/lib/group/unit/{group_id}"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/group/unit/{group_id}' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.group.delete(groupId, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const requestOptions = {
                        method: "DELETE",
                        headers: myHeaders,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/group/unit/{group_id}", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with no content"
        >
            ```json
            {
                "content": null,
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>

## Add Arke in a Group

<RestfulApiContainer>
    <div>
        The "Add Arke in a Group" endpoint create an association link between specified Arke and Group

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Add Arke in a Group"`}
            method="post"
            path="/group/unit/{group_id}/link/group/arke/unit/{arke_id}"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/group/unit/{group_id}/link/group/arke/unit/{arke_id}' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.group.addArke(groupId, arkeId)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const raw = JSON.stringify(data);

                    const requestOptions = {
                        method: "POST",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/group/unit/{group_id}/link/group/arke/unit/{arke_id}", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with Arke associated on group"
        >
            ```json
            {
                "content": {
                    {
                        "active": true,
                        "arke_id": "arke",
                        "id": "my_arke",
                        "inserted_at": "2023-09-14T10:03:48.000000Z",
                        "label": "My Arke",
                        "metadata": {},
                        "parameters": [],
                        "remote": false,
                        "type": "arke",
                        "updated_at": "2023-09-14T10:03:48.000000Z"
                    },
                },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>


## Remove Arke in a Group

<RestfulApiContainer>
    <div>
        The "Remove Arke in a Group" endpoint remove the specified arke in a group

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Remove Arke in a Group"`}
            method="delete"
            path="/group/unit/{group_id}/link/group/arke/unit/{arke_id}"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/group/unit/{group_id}/link/group/arke/unit/{arke_id}' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.group.removeArke(groupId, arkeId)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const raw = JSON.stringify(data);

                    const requestOptions = {
                        method: "DELETE",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/group/unit/{group_id}/link/group/arke/unit/{arke_id}", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with no content"
        >
            ```json
            {
                "content": null,
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>
