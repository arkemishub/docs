import {Tabs} from 'nextra/components'
import {RestfulApiRequest} from "../../components/restful-api/restful-api-request";
import {RestfulApiTable} from "../../components/restful-api/restful-api-table";
import {RestfulApiContainer} from "../../components/restful-api/restful-api-container";
import {RestfulApiResponse} from "../../components/restful-api/restful-api-response";
import {
    HEADERS_AUTH_ROWS,
    HEADERS_COLUMNS,
    HEADERS_ROWS,
    BODY_COLUMNS,
    HTTP_RESPONSE_COLUMNS,
    HTTP_RESPONSE_ROWS
} from '../../constants/restful-api'


## Create a member - Sign-up

<RestfulApiContainer>
    <div>
        The "Sign Up" endpoint facilitates the registration process for new users, allowing them to create an account
        within the system. This endpoint collects user-provided information such as username, email, and password,
        then validates and stores this data to enable access to the platform's features and services.

        The member are special entities under the group `arke_auth_member`, to create a member you have to provide all
        parameters required and additional special `arke_system_user` object.

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_AUTH_ROWS}
        />

        #### Body
        <RestfulApiTable
            columns={BODY_COLUMNS}
            rows={[
                {key: 'username', value: '<YOUR_USERNAME>' },
                {key: 'password', value: '<YOUR_PASSWORD>' },
                {key:  'arke_system_user', value: `{ password, username, email }` },
                {key: 'other_parameters', value: 'Other member parameters' },
            ]}
        />


        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Create a member"`}
            method="post"
            path="/api/lib/auth/{member_arke_id}/signup"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/auth/{member_arke_id}/signup' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H 'Content-Type: application/json' \
                    --data-raw '{
                        "username": "user@arkehub.com",
                        "password": "my_secret_password_123!",
                        "arke_system_user":{
                            "username":"user_username",
                            "password":"my_secret_password_123!",
                            "email":"user@arkehub.com"
                        }
                    }'
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.auth.signUp(arkeId, {
                    username: "user@arkehub.com",
                    password: "my_secret_password_123!",
                    arke_system_user:{
                    username: "user_username",
                    password: "my_secret_password_123!",
                    email: "user@arkehub.com"
                }
                }, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Content-Type", "application/json");

                    const raw = JSON.stringify({
                        "username": "user@arkehub.com",
                        "password": "my_secret_password_123!",
                        arke_system_user:{
                        username: "user_username",
                        password: "my_secret_password_123!",
                        email: "user@arkehub.com"
                    }
                    });

                    const requestOptions = {
                        method: "POST",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/{member_arke_id}/signup", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with created and autenticated member information"
        >
            ```json
            {
                "content": {
                "access_token": "eyJhbGciOiJIUzUxMiIsInR6IkpXVCJ9...",
                "arke_id": "super_admin",
                // ID of user stored on Arke System
                "arke_system_user": "c2e3b048-1a6a-11ee-908c-1a84faf9f6ef",
                "auth_token": null,
                "email": "user@arkehub.com",
                "first_access_time": "2024-04-02T20:05:30.919280Z",
                "first_name": "Admin",
                "id": "824135a6-52e6-11ee-9387-96e316d04b51",
                "inserted_at": "2023-09-14T10:07:27.000000Z",
                "last_access_time": "2024-04-03T16:20:12.452059Z",
                "last_name": "Super",
                "metadata": {},
                "refresh_token": "eyJhbGciOiJIUzUxMiIsInR5IkpXVCJ9...",
                ...additional member parameters
            },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>


## Authenticate a member - Sign-in Credentials

<RestfulApiContainer>
    <div>
        The "Authenticate a member" endpoint enables users to securely authenticate and access their accounts within
        the system. Users provide their credentials, a combination of a username/email and password, to validate their
        identity and gain access to the platform's features and resources.

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_AUTH_ROWS}
        />

        #### Body
        <RestfulApiTable
            columns={BODY_COLUMNS}
            rows={[
                {key: 'username', value: '<YOUR_USERNAME>' },
                {key: 'password', value: '<YOUR_PASSWORD>' },
            ]}
        />

        Upon successful authentication, the response contains an access token which should be included in subsequent requests
        for authorized access to protected resources.
        It's recommended to use HTTPS to ensure secure transmission of credentials.
        Always validate user input and sanitize data to prevent injection attacks.

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Authenticate a member"`}
            method="post"
            path="/api/lib/auth/signin"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/auth/signin' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H 'Content-Type: application/json' \
                    --data-raw '{
                        "username": "user@arkehub.com",
                        "password": "my_secret_password_123!",
                    }'
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.auth.signIn({
                        username: "user@arkehub.com",
                        password: "my_secret_password_123!",
                    }, 'credentials', config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Content-Type", "application/json");

                    const raw = JSON.stringify({
                    "username": "user@arkehub.com",
                    "password": "my_secret_password_123!",
                    });

                    const requestOptions = {
                    method: "POST",
                    headers: myHeaders,
                    body: raw,
                    redirect: "follow"
                };

                    fetch("https://arkehub.com/api/lib/auth/signin", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with member information"
        >
            ```json
            {
                "content": {
                    "access_token": "eyJhbGciOiJIUzUxMiIsInR6IkpXVCJ9...",
                    "arke_id": "super_admin",
                    // ID of user stored on Arke System
                    "arke_system_user": "c2e3b048-1a6a-11ee-908c-1a84faf9f6ef",
                    "auth_token": null,
                    "email": "user@arkehub.com",
                    "first_access_time": "2024-04-02T20:05:30.919280Z",
                    "first_name": "Admin",
                    "id": "824135a6-52e6-11ee-9387-96e316d04b51",
                    "inserted_at": "2023-09-14T10:07:27.000000Z",
                    "last_access_time": "2024-04-03T16:20:12.452059Z",
                    "last_name": "Super",
                    "metadata": {},
                    "refresh_token": "eyJhbGciOiJIUzUxMiIsInR5IkpXVCJ9...",
                    ...additional member parameters
                },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>

## Authenticate a member - One-Time-Password (OTP)

<RestfulApiContainer>
    <div>
        If OTP methods it enabled on your backend, the signin credentials needs an additional `otp` key to authenticate
        the member.

        To authenticate your account with OTP method, you have to call the sign in API two times.

        On the first time you can provide the standard username/password combination with the `otp` key set to null.
        If user exist, this request send an email to your account containing a alphanumeric/numeric code that you have
        to use in seconds request.

        Repeat the sign in API call setting the `otp` key with the code received by email. If it is correct the user will
        be authenticated and you'll receive the member session on the response

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_AUTH_ROWS}
        />

        #### Body
        <RestfulApiTable
            columns={BODY_COLUMNS}
            rows={[
                {key: 'username', value: '<YOUR_USERNAME>' },
                {key: 'password', value: '<YOUR_PASSWORD>' },
                {key: 'otp', value: 'null / <OTP_CODE>'  },
            ]}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Authenticate a member OTP"`}
            method="post"
            path="/api/lib/auth/signin"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/auth/signin' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H 'Content-Type: application/json' \
                    --data-raw '{
                        "username": "user@arkehub.com",
                        "password": "my_secret_password_123!",
                        "otp": null,
                    }'
                    ```

                    ```bash
                    curl --location 'https://arkehub.com/api/lib/auth/signin' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    --header 'Content-Type: application/json' \
                    --data-raw '{
                        "username": "user@arkehub.com",
                        "password": "my_secret_password_123!",
                        "otp": '12345',
                    }'
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.auth.signIn({
                        username: "user@arkehub.com",
                        password: "my_secret_password_123!",
                        otp: null
                    }, 'credentials', config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                    ```js
                    client.auth.signIn({
                        username: "user@arkehub.com",
                        password: "my_secret_password_123!",
                        otp: '12345'
                    }, 'credentials', config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Content-Type", "application/json");

                    const raw = JSON.stringify({
                        "username": "user@arkehub.com",
                        "password": "my_secret_password_123!",
                    });

                    const requestOptions = {
                        method: "POST",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/auth/signin", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with member information"
        >
            ```json
            {
                "content": {
                "access_token": "eyJhbGciOiJIUzUxMiIsInR6IkpXVCJ9...",
                "arke_id": "super_admin",
                // ID of user stored on Arke System
                "arke_system_user": "c2e3b048-1a6a-11ee-908c-1a84faf9f6ef",
                "auth_token": null,
                "email": "user@arkehub.com",
                "first_access_time": "2024-04-02T20:05:30.919280Z",
                "first_name": "Admin",
                "id": "824135a6-52e6-11ee-9387-96e316d04b51",
                "inserted_at": "2023-09-14T10:07:27.000000Z",
                "last_access_time": "2024-04-03T16:20:12.452059Z",
                "last_name": "Super",
                "metadata": {},
                "refresh_token": "eyJhbGciOiJIUzUxMiIsInR5IkpXVCJ9...",
                ...additional member parameters
            },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>

## Verify access token

<RestfulApiContainer>
    <div>
        The "Verify Token" API endpoint allows clients to verify the authenticity and validity of an access token
        issued by the system. Access tokens are commonly used for authentication and authorization purposes, and this
        endpoint provides a means to ensure that the token is valid and has not been tampered with.

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Verify token"`}
            method="post"
            path="/api/lib/auth/verify"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/auth/verify' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: "Bearer <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.auth.verifyToken(accessToken, {}, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "Bearer <ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const requestOptions = {
                        method: "POST",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/auth/verify", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response"
        >
            ```json
            {
                "content": null,
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>

## Refresh access token

<RestfulApiContainer>
    <div>
        The "Refresh Access Token" endpoint provides a mechanism for clients to obtain a new access token without
        requiring the user to re-enter their credentials. This endpoint is typically used to extend the validity of
        an existing access token, thereby maintaining the user's authenticated session without the need for frequent
        re-authentication.

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={[
                {value: 'status', label: 'Status code'},
                {value: 'description', label:'Description'}
            ]}
            rows={[
                {status: 200, description: 'OK' },
                {status: 401, description: 'Unauthorized' },
                {status: 500, description: 'Internal server error' }
            ]}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Verify token"`}
            method="post"
            path="/api/lib/auth/refresh"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/auth/refresh' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: "Bearer <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.auth.refreshToken(refreshToken, {}, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "Bearer <ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const requestOptions = {
                        method: "POST",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/auth/refresh", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response"
        >
            ```json
            {
                "content": null,
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>

## Recover & Reset password

<RestfulApiContainer>
    <div>
        The "Recover & Reset password" endpoint facilitates the recover password process for users, allowing them to reset their
        password. The recovery process occurs in two steps.

        On first step we have to call the recover password endpoint, this needs the user authentication's email to send an
        email containing a Token.

        On second step we have to call the reset password endpoint, this needs the new user password and token received by
        email.


        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={[{key: 'arke-project-key', value: '<YOUR_PROJECT_ID>' }]}
        />

        #### Recover password request Body
        <RestfulApiTable
            columns={BODY_COLUMNS}
            rows={[
                {key: 'email', value: '<YOUR_EMAIL>' },
            ]}
        />

        #### Reset password request Body
        <RestfulApiTable
            columns={BODY_COLUMNS}
            rows={[
                {key: 'new_password', value: '<YOUR_SECRET_PASSWORD>' },
            ]}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Recover password"`}
            method="post"
            path="/api/lib/auth/recover_password"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/auth/recover_password' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H 'Content-Type: application/json' \
                    --data-raw '{
                        "email":"user@arkehub.com"
                    }'
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.auth.recoverPassword("user@arkehub.com", config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Content-Type", "application/json");

                    const raw = JSON.stringify({
                        "email": "user@arkehub.com"
                    });

                    const requestOptions = {
                        method: "POST",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/auth/recover_password", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        {/*<RestfulApiResponse
            title="Response with confirm that email was sent"
        >
            ```json
            {
                "content": {},
                "messages": []
            }
            ```
        </RestfulApiResponse>*/}
        <br/>
        <RestfulApiRequest
            title={`Code samples for "Reset password"`}
            method="post"
            path="/api/lib/auth/reset_password/{token}"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/auth/reset_password/{token}' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H 'Content-Type: application/json' \
                    --data-raw '{
                        "new_password": "YOUR_SECRET_PASSWORD"
                    }'
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.auth.resetPassword("YOUR_SECRET_PASSWORD", token, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Content-Type", "application/json");

                    const raw = JSON.stringify({
                        "new_password": "YOUR_SECRET_PASSWORD"
                    });

                    const requestOptions = {
                        method: "POST",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/auth/reset_password/{token}", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        {/*<RestfulApiResponse
            title="Response with confirm that email is resetted"
        >
            ```json
            {
                "content": {},
                "messages": []
            }
            ```
        </RestfulApiResponse>*/}
    </div>
</RestfulApiContainer>
