import {Tabs} from 'nextra/components'
import {RestfulApiRequest} from "../../components/restful-api/restful-api-request";
import {RestfulApiTable} from "../../components/restful-api/restful-api-table";
import {RestfulApiContainer} from "../../components/restful-api/restful-api-container";
import {RestfulApiResponse} from "../../components/restful-api/restful-api-response";
import {
    BODY_COLUMNS,
    HEADERS_COLUMNS,
    HEADERS_ROWS,
    HTTP_RESPONSE_COLUMNS,
    HTTP_RESPONSE_ROWS,
    HTTP_RESPONSE_ROWS_DELETE,
    PARAMS_COLUMNS,
    PARAMS_ROWS
} from "../../constants/restful-api";

## Get Arke Struct

<RestfulApiContainer>
    <div>
        The "Get Arke Struct" endpoint return the structure of parameters of specified Arke

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### Params
        <RestfulApiTable
            columns={PARAMS_COLUMNS}
            rows={PARAMS_ROWS}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Get Arke Struct"`}
            method="get"
            path="/api/lib/arke/struct"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/arke/struct' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.arke.getAll(config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const requestOptions = {
                        method: "GET",
                        headers: myHeaders,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/arke/struct", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with Arke struct"
        >
            ```json
            {
                "content": {
                "label": "Arke",
                "parameters": [
                    {
                        "default": "arke",
                        "helper_text": "Type",
                        "id": "type",
                        "label": "Type",
                        "max_length": null,
                        "min_length": 2,
                        "multiple": false,
                        "required": true,
                        "strip": true,
                        "type": "string",
                        "values": null
                    },{
                        "default": false,
                        "helper_text": "Remote",
                        "id": "remote",
                        "label": "Remote",
                        "required": true,
                        "type": "boolean"
                    },
                ...
                ]
            },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>

## Get All Arke

<RestfulApiContainer>
    <div>
        The "Get All Arke" endpoint return the list of all available Arke inside the project

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Get all Arke"`}
            method="get"
            path="/api/lib/arke/unit"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/arke/unit' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.arke.getAll(config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const requestOptions = {
                    method: "GET",
                    headers: myHeaders,
                    redirect: "follow"
                };

                    fetch("https://arkehub.com/api/lib/arke/unit", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with Arke list"
        >
            ```json
            {
                "content": {
                    "count": 25,
                    "items": [
                        {
                            "active": true,
                            "arke_id": "arke",
                            "id": "super_admin",
                            "inserted_at": "2023-09-14T10:03:48.000000Z",
                            "label": "Super Admin",
                            "metadata": {},
                            "parameters": [],
                            "remote": false,
                            "type": "arke",
                            "updated_at": "2023-09-14T10:03:48.000000Z"
                        },
                        ...
                    ]
                },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>


## Get Arke

<RestfulApiContainer>
    <div>
        The "Get Arke" endpoint return details of the specified Arke

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Get Arke"`}
            method="get"
            path="/api/lib/arke/unit/{arke_id}"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/arke/unit/{arke_id}' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.arke.get(arkeId, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const requestOptions = {
                        method: "GET",
                        headers: myHeaders,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/arke/unit/{arke_id}", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with Arke details"
        >
            ```json
            {
                "content": {
                    {
                        "active": true,
                        "arke_id": "arke",
                        "id": "super_admin",
                        "inserted_at": "2023-09-14T10:03:48.000000Z",
                        "label": "Super Admin",
                        "metadata": {},
                        "parameters": [],
                        "remote": false,
                        "type": "arke",
                        "updated_at": "2023-09-14T10:03:48.000000Z"
                    },
                },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>


## Create Arke

<RestfulApiContainer>
    <div>
        The "Create Arke" endpoint create an Arke with the specified information

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### Body
        <RestfulApiTable
            columns={BODY_COLUMNS}
            rows={[
                {key: '<PARAMETER_ID>', value: '<VALUE>'},
                {key: '<PARAMETER_ID_2>', value: '<VALUE>'},
                {key: '<PARAMETER_ID_3>', value: '<VALUE>'}
            ]}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Create Arke"`}
            method="post"
            path="/api/lib/arke/unit"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/arke/unit/{arke_id}' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.arke.create(arkeId, data, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const raw = JSON.stringify(data);

                    const requestOptions = {
                        method: "POST",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/arke/unit/{arke_id}", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with Arke details created"
        >
            ```json
            {
                "content": {
                    {
                        "active": true,
                        "arke_id": "arke",
                        "id": "my_arke",
                        "inserted_at": "2023-09-14T10:03:48.000000Z",
                        "label": "My Arke",
                        "metadata": {},
                        "parameters": [],
                        "remote": false,
                        "type": "arke",
                        "updated_at": "2023-09-14T10:03:48.000000Z"
                    },
                },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>


## Edit Arke

<RestfulApiContainer>
    <div>
        The "Edit Arke" endpoint update the specified Arke information

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### Body
        <RestfulApiTable
            columns={BODY_COLUMNS}
            rows={[
                {key: '<PARAMETER_ID>', value: '<NEW_VALUE>'}
            ]}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Edit Arke"`}
            method="put"
            path="/api/lib/arke/unit/{arke_id}"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/arke/unit/{arke_id}' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.arke.edit(arkeId, data, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const raw = JSON.stringify(data);

                    const requestOptions = {
                        method: "PUT",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/arke/unit/{arke_id}", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with Arke details updated"
        >
            ```json
            {
                "content": {
            {
                "active": true,
                "arke_id": "arke",
                "id": "super_admin",
                "inserted_at": "2023-09-14T10:03:48.000000Z",
                "label": "Edited Super Admin Arke label",
                "metadata": {},
                "parameters": [],
                "remote": false,
                "type": "arke",
                "updated_at": "2023-09-14T10:03:48.000000Z"
            },
            },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>

## Delete Arke

<RestfulApiContainer>
    <div>
        The "Delete Arke" endpoint delete the specified Arke

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS_DELETE}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Delete Arke"`}
            method="delete"
            path="/api/lib/arke/unit/{arke_id}"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/arke/unit/{arke_id}' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.arke.delete(arkeId, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const requestOptions = {
                        method: "DELETE",
                        headers: myHeaders,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/arke/unit/{arke_id}", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with no content"
        >
            ```json
            {
                "content": null,
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>

## Custom functions

<RestfulApiContainer>
    <div>
        The "Custom function" endpoint allows you to invoke a custom function directly from your backend.

        On your arke backend you can create an elixir function in your `/backend/<ARKE>.ex` file, calling this endpoint
        you'll receive a response directly from this function.

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### Body
        <RestfulApiTable
            columns={BODY_COLUMNS}
            rows={[
                {key: '<CUSTOM_PARAMETER>', value: 'You can pass custom parameters required by the function'},
            ]}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Custom functions"`}
            method={["get", "post"]}
            path="/api/lib/{arke_id}/function/{function_name}"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/{arke_id}/{function_name}' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.arke.fn.get(arkeId, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));

                    client.arke.fn.post(arkeId, data, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const raw = JSON.stringify(data);

                    const requestOptions = {
                        method: "GET" / "POST",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/{arke_id}/{function_name}", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response of function"
        >
            ```json
            {
                "content": {
                    // Custom data response
                },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>
