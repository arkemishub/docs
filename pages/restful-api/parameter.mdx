import {Tabs} from 'nextra/components'
import {RestfulApiRequest} from "../../components/restful-api/restful-api-request";
import {RestfulApiTable} from "../../components/restful-api/restful-api-table";
import {RestfulApiContainer} from "../../components/restful-api/restful-api-container";
import {RestfulApiResponse} from "../../components/restful-api/restful-api-response";
import {
    BODY_COLUMNS,
    HEADERS_COLUMNS,
    HEADERS_ROWS,
    HTTP_RESPONSE_COLUMNS,
    HTTP_RESPONSE_ROWS,
    HTTP_RESPONSE_ROWS_DELETE,
    PARAMS_COLUMNS,
    PARAMS_ROWS
} from "../../constants/restful-api";

## Get All parameter types

<RestfulApiContainer>
    <div>
        The "Get All parameter types" endpoint return the list of all available types. By default these types are
        grouped under the `parameter` group, so we can retrieve them through a standard
        [Get All arke of Group](/restful-api/group#get-all-arke-of-group)

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### Params
        <RestfulApiTable
            columns={PARAMS_COLUMNS}
            rows={PARAMS_ROWS}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Get All parameter types"`}
            method="get"
            path="/api/lib/group/parameter/arke"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/group/parameter/arke' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.group.getAllArke('parameter',config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const requestOptions = {
                        method: "GET",
                        headers: myHeaders,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/group/parameter/arke", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with types list"
        >
            ```json
            {
                "content": {
                    "count": 12,
                    "items": [
                        {
                            "active": true,
                            "arke_id": "arke",
                            "id": "binary",
                            "label": "Binary",
                            "parameters": ["id","arke_id","default_binary","helper_text","only_run_time","required",...],
                            "type": "arke",
                            ...
                        },
                        {
                            "active": true,
                            "arke_id": "arke",
                            "id": "boolean",
                            "label": "Boolean",
                            "parameters": ["id","arke_id","default_binary","helper_text","only_run_time","required",...],
                            "type": "arke",
                            ...
                        },
                        {
                            "active": true,
                            "arke_id": "arke",
                            "id": "date",
                            "label": "Date",
                            "parameters": ["id","arke_id","default_binary","helper_text","only_run_time","required",...],
                            "type": "arke",
                            ...
                        },
                        ...
                    ]
                },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>

## Create Parameter

<RestfulApiContainer>
    <div>
        The "Create Parameter" endpoint create a Parameter with the specified information.

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### Body
        <RestfulApiTable
            columns={BODY_COLUMNS}
            rows={[
                {key: '<PARAMETER_ID>', value: '<VALUE>'},
                {key: '<PARAMETER_ID_2>', value: '<VALUE>'},
                {key: '<PARAMETER_ID_3>', value: '<VALUE>'}
            ]}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Create Arke"`}
            method="post"
            path="/api/lib/{parameter_type_id}/unit"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/{parameter_type_id}/unit' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.arke.create(parameterTypeId, data, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const raw = JSON.stringify(data);

                    const requestOptions = {
                        method: "POST",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/{parameter_type_id}/unit", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with Parameter details created"
        >
            ```json
            {
                "content": {
                    "arke_id": "string",
                    "default_string": null,
                    "format": "attribute",
                    "helper_text": null,
                    "id": "docs_parameter",
                    "inserted_at": "2024-04-04T18:59:26Z",
                    "is_primary": false,
                    "label": "Docs parameter",
                    "max_length": null,
                    "metadata": {},
                    "min_length": null,
                    "multiple": false,
                    "nullable": true,
                    "only_run_time": false,
                    "persistence": "arke_parameter",
                    "required": false,
                    "strip": false,
                    "unique": false,
                    "updated_at": "2024-04-04T18:59:26Z",
                    "values": null,
                },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>


## Edit Parameter

<RestfulApiContainer>
    <div>
        The "Edit Parameter" endpoint update the specified Parameter information

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### Body
        <RestfulApiTable
            columns={BODY_COLUMNS}
            rows={[
                {key: '<PARAMETER_ID>', value: '<NEW_VALUE>'}
            ]}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Edit Parameter"`}
            method="put"
            path="/api/lib/{parameter_type_id}/unit/{parameter_id}"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/{parameter_type_id}/unit/{parameter_id}' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.arke.edit(parameterId, data, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const raw = JSON.stringify(data);

                    const requestOptions = {
                        method: "PUT",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/{parameter_type_id}/unit/{parameter_id}", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with Parameter details updated"
        >
            ```json
            {
                "content": {
                    {
                        "arke_id": "string",
                        "default_string": null,
                        "format": "attribute",
                        "helper_text": null,
                        "label": "Edited Docs parameter",
                        "id": "docs_parameter",
                        "inserted_at": "2024-04-04T18:59:26Z",
                        "updated_at": "2024-04-04T19:59:26Z",
                    },
                },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>

## Delete Parameter

<RestfulApiContainer>
    <div>
        The "Delete Parameter" endpoint delete the specified Parameter

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS_DELETE}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Delete Parameter"`}
            method="delete"
            path="/api/lib/{parameter_type_id}/unit/{parameter_id}"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/{parameter_type_id}/unit/{parameter_id}' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    client.arke.delete(parameterId, config)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const requestOptions = {
                        method: "DELETE",
                        headers: myHeaders,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/{parameter_type_id}/unit/{parameter_id}", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with no content"
        >
            ```json
            {
                "content": null,
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>
