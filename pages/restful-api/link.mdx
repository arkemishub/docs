import {Tabs} from 'nextra/components'
import {RestfulApiRequest} from "../../components/restful-api/restful-api-request";
import {RestfulApiTable} from "../../components/restful-api/restful-api-table";
import {RestfulApiContainer} from "../../components/restful-api/restful-api-container";
import {RestfulApiResponse} from "../../components/restful-api/restful-api-response";
import {
    BODY_COLUMNS,
    HEADERS_COLUMNS,
    HEADERS_ROWS,
    HTTP_RESPONSE_COLUMNS,
    HTTP_RESPONSE_ROWS, HTTP_RESPONSE_ROWS_DELETE
} from "../../constants/restful-api";

## Create Link

<RestfulApiContainer>
    <div>
        The "Create Link" endpoint create an association link between specified units

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Create Link"`}
            method="post"
            path="/{parent_arke_id}/unit/{parent_unit_id}/link/{link_id}/{child_arke_id}/unit/{child_unit_id}"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/{parent_arke_id}/unit/{parent_unit_id}/link/{link_id}/{child_arke_id}/unit/{child_unit_id}' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const parent = { arkeId: 'parent_arke_id', id: 'parent_unit_id' };
                    const child = { arkeId: 'child_arke_id', id: 'child_unit_id' };
                    const linkId = 'link';
                    client.topology.addLink(parent, linkId, child)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const raw = JSON.stringify(data);

                    const requestOptions = {
                        method: "POST",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib//{parent_arke_id}/unit/{parent_unit_id}/link/{link_id}/{child_arke_id}/unit/{child_unit_id}", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with association"
        >
            ```json
            {
                "content": {
                    {
                        "child_id": "986233a4-4374-4590-9690-3b3e980ed64a";
                        "parent_id": "99d77c36-cf71-4acd-b744-09b2614cc11d";
                        "type": "link";
                    },
                },
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>


## Remove Link

<RestfulApiContainer>
    <div>
        The "Remove Link" endpoint remove the link between specified units

        #### Headers
        <RestfulApiTable
            columns={HEADERS_COLUMNS}
            rows={HEADERS_ROWS}
        />

        #### HTTP Response status
        <RestfulApiTable
            columns={HTTP_RESPONSE_COLUMNS}
            rows={HTTP_RESPONSE_ROWS}
        />

    </div>
    <div>
        <RestfulApiRequest
            title={`Code samples for "Remove Link in a Group"`}
            method="delete"
            path="/parent_arke_id/unit/{parent_unit_id}/link/{link_id}/{child_arke_id}/unit/{child_unit_id}"
        >
            <Tabs items={['cURL', '@arkejs/client', 'fetch']}>
                <Tabs.Tab>
                    ```bash
                    curl --location 'https://arkehub.com/api/lib/parent_arke_id/unit/{parent_unit_id}/link/{link_id}/{child_arke_id}/unit/{child_unit_id}' \
                    -H "Arke-Project-Key: <YOUR_PROJECT_ID>" \
                    -H "Authorization: <ACCESS_TOKEN>" \
                    -H 'Content-Type: application/json' \
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const parent = { arkeId: 'parent_arke_id', id: 'parent_unit_id' };
                    const child = { arkeId: 'child_arke_id', id: 'child_unit_id' };
                    const linkId = 'link';
                    client.topology.removeLink(parent, linkId, child)
                    .then((response) => console.log(response.data)
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
                <Tabs.Tab>
                    ```js
                    const myHeaders = new Headers();
                    myHeaders.append("arke-project-key", "<YOUR_PROJECT_ID>");
                    myHeaders.append("Authorization", "<ACCESS_TOKEN>");
                    myHeaders.append("Content-Type", "application/json");

                    const raw = JSON.stringify(data);

                    const requestOptions = {
                        method: "DELETE",
                        headers: myHeaders,
                        body: raw,
                        redirect: "follow"
                    };

                    fetch("https://arkehub.com/api/lib/parent_arke_id/unit/{parent_unit_id}/link/{link_id}/{child_arke_id}/unit/{child_unit_id}", requestOptions)
                    .then((response) => response.text())
                    .then((result) => console.log(result.data))
                    .catch((error) => console.error(error));
                    ```
                </Tabs.Tab>
            </Tabs>
        </RestfulApiRequest>

        <RestfulApiResponse
            title="Response with no content"
        >
            ```json
            {
                "content": null,
                "messages": []
            }
            ```
        </RestfulApiResponse>
    </div>
</RestfulApiContainer>
