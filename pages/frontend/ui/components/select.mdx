import {Tabs} from "nextra/components"
import SelectPreview from "@/components/docs-ui/select/select-preview";
import SelectCode from "@/components/docs-ui/select/select-code.mdx";

# Select
Displays a list of options for the user to pick from—triggered by a button.

### Installation
```bash
npx arke-ui@latest add select
```

<Tabs items={['Preview', 'Code']}>
    <Tabs.Tab>
        <SelectPreview />
    </Tabs.Tab>
    <Tabs.Tab>
        <SelectCode />
    </Tabs.Tab>
</Tabs>


